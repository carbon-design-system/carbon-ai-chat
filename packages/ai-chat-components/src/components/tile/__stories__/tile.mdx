import { ArgTypes, Markdown, Meta } from "@storybook/blocks";
import { cdnJs, cdnCss } from "../../../globals/internal/storybook-cdn";
import * as TileStories from "./tile.stories";

<Meta of={TileStories} />

# Extended Button

[Insert blurb of component and its usage here]

## Getting started

Here's a quick example to get you started.

### JS (via import)

```javascript
import "@carbon/web-components/es/components/tile/index.js";
```

<Markdown>{`${cdnJs({ components: ["tile"] })}`}</Markdown>
<Markdown>{`${cdnCss()}`}</Markdown>

### HTML

```html
<cds-aichat-tile> Extended tile </cds-aichat-tile>
```

## `<cds-aichat-tile>` attributes, properties and events

Note: For `boolean` attributes, `true` means simply setting the attribute (e.g.
`<cds-aichat-tile open>`) and `false` means not setting the attribute (e.g.
`<cds-aichat-tile>` without `open` attribute).

<ArgTypes of="cds-aichat-tile" />
