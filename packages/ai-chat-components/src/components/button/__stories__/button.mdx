import { ArgTypes, Markdown, Meta } from '@storybook/addon-docs/blocks';
import { cdnJs, cdnCss } from "../../../globals/internal/storybook-cdn";
import * as ChatButtonStories from "./button.stories";

<Meta of={ChatButtonStories} />

# Chat Button

[Insert blurb of component and its usage here]

## Getting started

Here's a quick example to get you started.

### JS (via import)

```javascript
import "@carbon/web-components/es/components/button/index.js";
```

<Markdown>{`${cdnJs({ components: ["button"] })}`}</Markdown>
<Markdown>{`${cdnCss()}`}</Markdown>

### HTML

```html
<cds-aichat-button> Extended button </cds-aichat-button>
```

## `<cds-aichat-button>` attributes, properties and events

Note: For `boolean` attributes, `true` means simply setting the attribute (e.g.
`<cds-aichat-button open>`) and `false` means not setting the attribute (e.g.
`<cds-aichat-button>` without `open` attribute).

<ArgTypes of="cds-aichat-button" />
